#!/system/bin/sh

# ==============================================================================
# [ Flux Cache Manager ]
# Description: Rules generation, kernel feature detection, and environment fingerprinting.
# ==============================================================================

# Strict error handling
set -eu
[ -n "${BASH_VERSION:-}" ] && set -o pipefail

readonly SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
. "${SCRIPT_DIR}/const"
. "${SCRIPT_DIR}/log"
. "${SCRIPT_DIR}/config"
. "${SCRIPT_DIR}/rules"

readonly LOG_COMPONENT="Cach"

# ==============================================================================
# [ Atomic Write Helper ]
# ==============================================================================

# Generic atomic write helper
# Usage: _atomic_write <target_file> <command> [args...]
_atomic_write() {
    local target_file="${1}"; shift
    local tmp_file; tmp_file=$(mktemp "${target_file}.XXXXXX") || return 1
    trap "rm -f '${tmp_file}'" EXIT

    if "$@" > "${tmp_file}"; then
        chmod 644 "${tmp_file}"
        mv -f "${tmp_file}" "${target_file}"
        trap - EXIT
        return 0
    else
        log_error "Failed to generate cache: ${target_file##*/}"
        return 1
    fi
}

# ==============================================================================
# [ Rules Cache Management ]
# ==============================================================================

_save_rules_cache() {
    mkdir -p "$(dirname "${CACHE_RULES_V4_FILE}")" 2>/dev/null

    # 1. IPTables Rules (The primary driver)
    _atomic_write "${CACHE_RULES_V4_FILE}" generate_all_rules "4" || return 1
    _atomic_write "${CACHE_CLEANUP_V4_FILE}" generate_all_cleanup_rules "4" || return 1

    if [ "${PROXY_IPV6}" = "1" ]; then
        _atomic_write "${CACHE_RULES_V6_FILE}" generate_all_rules "6" || return 1
        _atomic_write "${CACHE_CLEANUP_V6_FILE}" generate_all_cleanup_rules "6" || return 1
    fi

    log_debug "Iptables Rules cache saved"
    return 0
}

# ==============================================================================
# [ Kernel Feature Detection Cache ]
# ==============================================================================

# Detect and cache available kernel networking features to optimize rule selection.
# Prioritizes /proc/config.gz if available, falling back to experimental probe logic.
_save_kernel_cache() {
    local current_kernel
    current_kernel=$(uname -r 2>/dev/null)
    if [ -f "${KERNEL_CACHE_FILE}" ]; then
        local cached_kernel
        cached_kernel=$(grep "^#kernel:" "${KERNEL_CACHE_FILE}" 2>/dev/null | cut -d: -f2-)

        if [ "${cached_kernel}" = "${current_kernel}" ]; then
            log_debug "Kernel Cache valid"
            set -a; . "${KERNEL_CACHE_FILE}"; set +a
            return 0
        fi
    fi
    log_debug "Building Kernel Capability Cache..."
    # Detection: try /proc/config.gz first, then fall back to trial/error detection
    local features
    if [ -f /proc/config.gz ]; then
        features=$(zcat /proc/config.gz 2>/dev/null | awk '
            /CONFIG_NETFILTER_XT_TARGET_TPROXY=[ym]/ { t=1 }
            /CONFIG_NETFILTER_XT_MATCH_OWNER=[ym]/ { o=1 }
            /CONFIG_NETFILTER_XT_MATCH_MARK=[ym]/ { m=1 }
            /CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=[ym]/ { a=1 }
            /CONFIG_NETFILTER_XT_MATCH_CONNTRACK=[ym]/ { c=1 }
            /CONFIG_IP6_NF_NAT=[ym]/ { i=1 }
            END {
                printf "KFEAT_TPROXY=%d;KFEAT_OWNER=%d;KFEAT_MARK=%d;KFEAT_ADDRTYPE=%d;KFEAT_CONNTRACK=%d;KFEAT_IPV6_NAT=%d",
                       t+0, o+0, m+0, a+0, c+0, i+0
            }
        ')
    else
        log_warn "/proc/config.gz missing, falling back to module/trial detection"
        local t=0 o=0 m=0 a=0 c=0 i=0
        # Simple match detection via /proc/net/ip_tables_matches
        grep -qw "tproxy" /proc/net/ip_tables_targets 2>/dev/null && t=1
        grep -qw "owner" /proc/net/ip_tables_matches 2>/dev/null && o=1
        grep -qw "mark" /proc/net/ip_tables_matches 2>/dev/null && m=1
        grep -qw "addrtype" /proc/net/ip_tables_matches 2>/dev/null && a=1
        grep -qw "conntrack" /proc/net/ip_tables_matches 2>/dev/null && c=1
        # Trial detection for cases where modules aren't listed yet
        [ "${t}" = "0" ] && iptables -t mangle -A OUTPUT -p tcp -j TPROXY --on-port 1536 --tproxy-mark 0x1 2>/dev/null && { t=1; iptables -t mangle -D OUTPUT -p tcp -j TPROXY --on-port 1536 --tproxy-mark 0x1 2>/dev/null; }
        features="KFEAT_TPROXY=$t;KFEAT_OWNER=$o;KFEAT_MARK=$m;KFEAT_ADDRTYPE=$a;KFEAT_CONNTRACK=$c;KFEAT_IPV6_NAT=1"
    fi
    # Batch export and prepare file content
    local content="# Kernel Feature Cache - Auto Generated\n#kernel:${current_kernel}\n"
    local old_ifs="${IFS}"
    IFS=';'
    for feat in ${features}; do
        export "${feat}"
        content="${content}${feat}\n"
    done
    IFS="${old_ifs}"

    mkdir -p "$(dirname "${KERNEL_CACHE_FILE}")" 2>/dev/null

    if _atomic_write "${KERNEL_CACHE_FILE}" printf '%b' "$content"; then
        log_debug "Kernel Cache built successfully"
        return 0
    else
        return 1
    fi
}

# ==============================================================================
# [ Meta Cache (Environmental Fingerprint) ]
# ==============================================================================

# Verify if the current environment matches the cached state.
# Compares a cryptographic-like signature derived from file mtimes and kernel version.
_get_mtime() {
    local file="${1}"
    if [ -f "${file}" ]; then
        (stat -c %Y "${file}" 2>/dev/null || stat -f %m "${file}" 2>/dev/null || printf "0") | tr -d '\n\r'
    else
        printf "0"
    fi
    return 0
}

_get_version_code() {
    grep "^versionCode=" "${PROP_FILE}" 2>/dev/null | cut -d= -f2-
}

_get_meta_signature() {
    [ -n "${_CACHED_META_SIG:-}" ] && { printf '%s\n' "${_CACHED_META_SIG}"; return 0; }
    _CACHED_META_SIG=$(printf "vcode:%s\nsettings_mtime:%s\nconfig_mtime:%s\npackages_mtime:%s\nkernel:%s" \
        "$(_get_version_code)" \
        "$(_get_mtime "${SETTINGS_FILE}")" \
        "$(_get_mtime "${CONFIG_FILE}")" \
        "$(_get_mtime "${PACKAGES_LIST}")" \
        "$(uname -r 2>/dev/null)")
    printf '%s\n' "${_CACHED_META_SIG}"
    return 0
}

_is_cache_valid() {
    # Fast check: core files must exist
    for f in "${CACHE_META_FILE}" "${CACHE_CONFIG_FILE}" "${KERNEL_CACHE_FILE}" "${CACHE_RULES_V4_FILE}"; do
        if [ ! -f "${f}" ]; then
            log_debug "Cache invalid: missing core file ${f##*/}"
            return 1
        fi
    done
    local current_sig
    current_sig=$(_get_meta_signature)
    local cached_sig
    cached_sig=$(awk '
        /^#build_ts:/ { next }
        /^#/ { sub(/^#/, ""); print }
    ' "${CACHE_META_FILE}" 2>/dev/null)

    if [ "${current_sig}" = "${cached_sig}" ]; then
        log_debug "Cache is valid"
        return 0
    fi
    # Debug mismatch
    log_debug "Cache signature mismatch"
    return 1
}

# Save a snapshot of the current environment state for future validation.
_save_meta_cache() {
    local sig
    sig=$(_get_meta_signature)
    mkdir -p "$(dirname "${CACHE_META_FILE}")" 2>/dev/null
    _gen_meta() {
        printf '%s\n' "$sig" | sed 's/^/#/'
        printf '#build_ts:%s\n' "$(date +%s)"
    }
    if _atomic_write "${CACHE_META_FILE}" _gen_meta; then
        log_debug "Meta cache saved"
        return 0
    else
        return 1
    fi
}

# ==============================================================================
# [ Config Cache Generation ]
# ==============================================================================

_normalize_list() {
    printf '%s' "${1}" | awk '{for(i=1;i<=NF;i++) printf "%s%s", $i, (i==NF?"":" ")}'
}

_save_config_cache() {
    mkdir -p "$(dirname "${CACHE_CONFIG_FILE}")" 2>/dev/null
    local _app_list
    _app_list=$(_normalize_list "${APP_LIST}")
    _gen_config() {
        cat << EOF
# Flux Config Cache - Auto Generated
SUBSCRIPTION_URL="${SUBSCRIPTION_URL}"
UPDATE_TIMEOUT=${UPDATE_TIMEOUT}
RETRY_COUNT=${RETRY_COUNT}
UPDATE_INTERVAL=${UPDATE_INTERVAL}
PREF_CLEANUP_EMOJI=${PREF_CLEANUP_EMOJI}
LOG_LEVEL=${LOG_LEVEL}
LOG_MAX_SIZE=${LOG_MAX_SIZE}
CORE_USER=${CORE_USER}
CORE_GROUP=${CORE_GROUP}
CORE_TIMEOUT=${CORE_TIMEOUT}
MOBILE_INTERFACE="${MOBILE_INTERFACE}"
WIFI_INTERFACE="${WIFI_INTERFACE}"
HOTSPOT_INTERFACE="${HOTSPOT_INTERFACE}"
USB_INTERFACE="${USB_INTERFACE}"
HOTSPOT_V4_RANGE="${HOTSPOT_V4_RANGE}"
HOTSPOT_V6_RANGE="${HOTSPOT_V6_RANGE}"
PROXY_PORT=${PROXY_PORT}
FAKEIP_V4_RANGE=${FAKEIP_V4_RANGE}
FAKEIP_V6_RANGE=${FAKEIP_V6_RANGE}
PROXY_MOBILE=${PROXY_MOBILE}
PROXY_WIFI=${PROXY_WIFI}
PROXY_HOTSPOT=${PROXY_HOTSPOT}
PROXY_USB=${PROXY_USB}
PROXY_IPV6=${PROXY_IPV6}
TABLE_ID=${TABLE_ID}
IPV4_MARK=${IPV4_MARK}
IPV6_MARK=${IPV6_MARK}
BYPASS_MARK=${BYPASS_MARK}
ROUTING_MARK="${ROUTING_MARK}"
APP_PROXY_MODE=${APP_PROXY_MODE}
APP_LIST="${_app_list}"
SKIP_CHECK_FEATURE=${SKIP_CHECK_FEATURE}
MSS_CLAMP_ENABLE=${MSS_CLAMP_ENABLE}
EXCLUDE_INTERFACES="${EXCLUDE_INTERFACES}"
EOF
    }
    if _atomic_write "${CACHE_CONFIG_FILE}" _gen_config; then
        log_debug "Config cache saved"
        return 0
    else
        return 1
    fi
}


# ==============================================================================
# [ Main Cache Orchestration ]
# ==============================================================================

# Sequential build of all project caches.
build_all_caches() {
    run "Validate system config" validate_all || return 1
    run "Save kernel capability" _save_kernel_cache || return 1
    run "Save normalized config" _save_config_cache || return 1
    run "Save iptables rules" _save_rules_cache || return 1
    run "Update meta signature" _save_meta_cache || return 1
    return 0
}

# Entry Point (Standalone Execution)

check_cache() {
    if _is_cache_valid; then
        log_info "All caches are valid, skipping build"
    else
        log_info "Cache invalid or missing, rebuilding..."
        if build_all_caches; then
            return 0
        else
            log_error "Cache rebuild failed"
            return 1
        fi
    fi
    return 0
}

main() {
    local action="${1:-}"

    case "${action}" in
        check)
            check_cache
            ;;
        build)
            build_all_caches
            ;;
        *)
            check_cache
            ;;
    esac
}

main "$@"
